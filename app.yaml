application: query2json
version: 1
runtime: python
api_version: 1

handlers:
- url: /markup
  static_dir: markup

- url: /styles
  static_dir: styles

- url: /scripts
  static_dir: scripts

- url: /images
  static_dir: images

- url: /(:?.+)?
  script: main.py
